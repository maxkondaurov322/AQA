name: Run autotests

on:
  workflow_dispatch:
    deployment_target:
      decription: select tests to run
      required: true
      type: choice
      default: all
      options:
        -all

jobs:
  run-tests:
    runs-on: ubuntu-latest
    name: Tests
    steps: 
      - name: Copy source code
        uses: actions/checkout@v4
      - name: Set up python env
        uses: actions/setup-python@v5
        with: 
          python-version: "3.14"
      - name: install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest -v 
